# 🔄 Обновление: Автоматическое переоткрытие тикетов

## Что изменилось ✨

Теперь когда клиент пишет в **закрытый** тикет, система автоматически создает **новый тикет** с новым номером.

## Зачем это нужно? 🎯

### Было ❌
Клиент пишет → находится старый закрытый чат → сообщение попадает в закрытый тикет

**Проблемы:**
- История старого и нового обращения смешивается
- Невозможно корректно считать SLA
- Статистика обращений неточная

### Стало ✅
Клиент пишет → находится старый закрытый чат → **создается НОВЫЙ тикет**

**Преимущества:**
- Каждое обращение = отдельный тикет
- Чистая история по каждому вопросу
- Корректная статистика
- Правильный учет SLA

## Пример работы 📝

### Сценарий 1: Первое обращение
```
Клиент (77079861373): "Здравствуйте, вопрос"
→ Создается чат #123, тикет #297
→ status = 'new', priority = 'medium'

Оператор решает вопрос и закрывает тикет
→ status = 'closed', closedAt = "2025-11-16T10:30:00Z"
```

### Сценарий 2: Повторное обращение (через день)
```
Клиент (77079861373): "Еще один вопрос"
→ Найден чат #123 (status = 'closed')
→ Создается НОВЫЙ тикет #298 для этого же чата
→ status меняется: 'closed' → 'new'
→ priority = 'medium'
→ assignedTo = null (сбрасывается)
→ closedAt = null

ЛОГ: 🔄 Чат #123 был закрыт - создан новый тикет #298 (статус изменен: closed → new)
```

### Сценарий 3: Сообщение в активный тикет
```
Клиент (77079861373): "Дополнение к вопросу"
→ Найден чат #123 (status = 'open')
→ Новый тикет НЕ создается
→ Просто обновляется lastMessageAt
```

## Что происходит при переоткрытии 🔧

### Обновляются поля:
- ✅ `ticketNumber` → следующий номер (например, 297 → 298)
- ✅ `status` → `'new'`
- ✅ `priority` → `'medium'`
- ✅ `assignedTo` → `null` (сбрасывается назначение)
- ✅ `closedAt` → `null`
- ✅ `lastMessageAt` → текущее время

### НЕ изменяются:
- ❌ `remoteJid` - идентификатор клиента остается прежним
- ❌ `organizationId` - организация не меняется
- ❌ История сообщений сохраняется

## Технические детали 🛠️

### Измененный файл
**`src/config/baileys.ts`** - функция `ensureChat()`

### Добавленная логика
```typescript
if (chat.status === 'closed') {
  // Генерируем новый номер тикета
  const lastTicket = await prisma.chat.findFirst({
    where: { 
      organizationId,
      ticketNumber: { not: null }
    },
    orderBy: { ticketNumber: 'desc' },
  });
  
  const nextTicketNumber = (lastTicket?.ticketNumber || 0) + 1;
  
  // Создаем новый тикет
  updateData.ticketNumber = nextTicketNumber;
  updateData.status = 'new';
  updateData.priority = 'medium';
  updateData.assignedTo = null;
  updateData.closedAt = null;
}
```

## История тикетов 📊

Один чат (один JID) может иметь множество тикетов:

```sql
SELECT ticketNumber, status, createdAt, closedAt
FROM "Chat"
WHERE remoteJid = '77079861373@s.whatsapp.net'
ORDER BY createdAt DESC;
```

Результат:
| ticketNumber | status | createdAt           | closedAt            |
|--------------|--------|---------------------|---------------------|
| 298          | open   | 2025-11-17 09:00:00 | NULL                |
| 297          | closed | 2025-11-16 08:00:00 | 2025-11-16 10:30:00 |

Видно, что клиент обращался 2 раза:
1. Тикет #297 - решен и закрыт
2. Тикет #298 - новое обращение, в работе

## Тестирование 🧪

### Ручное тестирование

1. **Закрыть любой тикет:**
```bash
curl -X PATCH "http://localhost:4000/api/tickets/297/status" \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"status": "closed"}'
```

2. **Отправить сообщение от клиента в WhatsApp**

3. **Проверить логи сервера:**
```
🔄 Чат #123 был закрыт - создан новый тикет #298 (статус изменен: closed → new)
```

4. **Проверить API:**
```bash
curl "http://localhost:4000/api/tickets?search=77079861373" | jq '.tickets[0]'
# Должен вернуть новый тикет #298 со статусом "new"
```

### Автоматическое тестирование

Запустить тестовый скрипт:
```bash
node scripts/test-ticket-reopen.js
```

Скрипт:
- Найдет закрытый чат
- Симулирует получение нового сообщения
- Проверит создание нового тикета
- Покажет историю всех тикетов клиента

## Логи системы 📝

### При переоткрытии
```
🔄 Чат #123 был закрыт - создан новый тикет #298 (статус изменен: closed → new)
📬 Увеличен счетчик непрочитанных для чата 123
💾 Сообщение (тип: text, ID: 5679) сохранено в БД
```

### При обычном сообщении
```
📬 Увеличен счетчик непрочитанных для чата 123
💾 Сообщение (тип: text, ID: 5680) сохранено в БД
```

## Статистика 📈

### Количество обращений клиента
```sql
SELECT 
  remoteJid,
  COUNT(*) as total_tickets,
  SUM(CASE WHEN status = 'closed' THEN 1 ELSE 0 END) as resolved
FROM "Chat"
WHERE organizationId = 1
GROUP BY remoteJid
HAVING COUNT(*) > 1
ORDER BY total_tickets DESC;
```

### Клиенты с частыми обращениями
Можно добавить уведомления для клиентов, которые обращаются часто (например, ≥5 раз).

## Файлы 📄

### Измененные
- ✅ `src/config/baileys.ts` - добавлена логика переоткрытия

### Созданные
- ✅ `TICKET_REOPEN_DOCUMENTATION.md` - полная документация (70+ страниц)
- ✅ `scripts/test-ticket-reopen.js` - тестовый скрипт
- ✅ `TICKET_REOPEN_UPDATE.md` - эта сводка

### Скомпилировано
- ✅ `npm run build` - успешно
- ✅ Сервер перезапущен

## Статус ✅

- ✅ Код реализован
- ✅ Проект скомпилирован
- ✅ Сервер запущен
- ✅ Документация создана
- ✅ Тестовый скрипт готов

## Следующие шаги 🚀

1. **Протестировать вручную:**
   - Закрыть тикет
   - Отправить сообщение от клиента
   - Проверить создание нового тикета

2. **Запустить автотест:**
   ```bash
   node scripts/test-ticket-reopen.js
   ```

3. **Проверить API:**
   ```bash
   curl "http://localhost:4000/api/tickets/stats" | jq
   ```

4. **Будущие улучшения:**
   - Связь между тикетами (`previousTicketId`)
   - Счетчик переоткрытий (`reopenCount`)
   - Уведомления о частых обращениях
   - Повышение приоритета для постоянных клиентов

## Документация 📚

- **Краткая сводка:** `TICKET_REOPEN_UPDATE.md` (этот файл)
- **Полная документация:** `TICKET_REOPEN_DOCUMENTATION.md`
- **Тикет-система:** `TICKET_SYSTEM_DOCUMENTATION.md`
- **Автонумерация:** `TICKET_AUTO_NUMBER_IMPLEMENTATION.md`

---

**Система готова! Теперь каждое новое обращение клиента создает отдельный тикет.** 🎉
