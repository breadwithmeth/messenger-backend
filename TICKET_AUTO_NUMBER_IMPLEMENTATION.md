# 🎫 Автогенерация номеров тикетов - Документация

## Обзор изменений

Реализована автоматическая генерация номеров тикетов для всех новых чатов. Теперь при получении первого сообщения от клиента автоматически создается тикет с уникальным номером.

---

## 🔧 Технические детали

### Изменения в коде

**Файл:** `src/config/baileys.ts`  
**Функция:** `ensureChat()`

#### До изменений:

```typescript
chat = await prisma.chat.create({
  data: {
    organizationId,
    receivingPhoneJid: myJidNormalized,
    remoteJid: normalizedRemoteJid,
    organizationPhoneId: organizationPhoneId,
    name: name || normalizedRemoteJid.split('@')[0],
    isGroup: isJidGroup(normalizedRemoteJid),
    lastMessageAt: new Date(),
  },
});
```

#### После изменений:

```typescript
// Генерируем следующий номер тикета для организации
const lastTicket = await prisma.chat.findFirst({
  where: { 
    organizationId,
    ticketNumber: { not: null }
  },
  orderBy: { ticketNumber: 'desc' },
  select: { ticketNumber: true },
});

const nextTicketNumber = (lastTicket?.ticketNumber || 0) + 1;

chat = await prisma.chat.create({
  data: {
    organizationId,
    receivingPhoneJid: myJidNormalized,
    remoteJid: normalizedRemoteJid,
    organizationPhoneId: organizationPhoneId,
    name: name || normalizedRemoteJid.split('@')[0],
    isGroup: isJidGroup(normalizedRemoteJid),
    lastMessageAt: new Date(),
    // Тикет-система: автоматически создаем тикет для нового чата
    ticketNumber: nextTicketNumber,
    status: 'new',
    priority: 'medium',
  },
});
```

---

## 🚀 Логика работы

### 1. Поиск максимального номера

При создании нового чата система:

1. Ищет последний тикет в организации:
   ```sql
   SELECT ticketNumber 
   FROM Chat 
   WHERE organizationId = ? AND ticketNumber IS NOT NULL
   ORDER BY ticketNumber DESC 
   LIMIT 1
   ```

2. Инкрементирует номер:
   ```javascript
   const nextTicketNumber = (lastTicket?.ticketNumber || 0) + 1;
   ```

### 2. Создание тикета

Новый чат создается с полями:

| Поле | Значение | Описание |
|------|----------|----------|
| `ticketNumber` | auto-increment | Уникальный номер для организации |
| `status` | `'new'` | Новый тикет, требует внимания |
| `priority` | `'medium'` | Средний приоритет по умолчанию |

### 3. Уникальность

- **По организации**: Каждая организация имеет свою последовательность номеров (1, 2, 3...)
- **Не глобально**: Разные организации могут иметь тикеты с одинаковыми номерами
- **Constraint**: Уникальное ограничение `(organizationId, ticketNumber)` в БД

---

## 📊 Миграция существующих чатов

Для существующих чатов без номеров создан скрипт миграции.

### Скрипт миграции

**Файл:** `scripts/migrate-ticket-numbers.js`

**Запуск:**
```bash
node scripts/migrate-ticket-numbers.js
```

### Что делает скрипт:

1. **Находит организации** с чатами без номеров
2. **Для каждой организации:**
   - Находит максимальный существующий `ticketNumber`
   - Получает все чаты без номеров, отсортированные по `createdAt`
   - Присваивает последовательные номера начиная с `max + 1`
   - Устанавливает `status = 'new'` и `priority = 'medium'`
3. **Выводит статистику** по завершению

### Пример вывода:

```
🚀 Начинаем миграцию номеров тикетов...

📊 Найдено организаций с чатами без номеров: 1

📋 Организация ID 1:
   - Чатов без номеров: 296
   - Начальный номер тикета: 1
   ✅ Обновлено: 296 чатов

✨ Миграция завершена успешно!
   Всего обновлено чатов: 296

📊 Финальная статистика:
   Чатов С номерами: 296
   Чатов БЕЗ номеров: 0
```

---

## 🔍 Проверка работы

### 1. API тикетов теперь возвращает все чаты

**До:**
```json
{
  "tickets": [],
  "pagination": {
    "total": 0,
    "page": 1,
    "limit": 20,
    "pages": 0
  }
}
```

**После:**
```json
{
  "tickets": [...],
  "pagination": {
    "total": 296,
    "page": 1,
    "limit": 20,
    "pages": 60
  }
}
```

### 2. Статистика

```bash
GET /api/tickets/stats
```

**Ответ:**
```json
{
  "total": 296,
  "byStatus": {
    "new": 295,
    "closed": 1
  },
  "byPriority": {
    "medium": 296
  }
}
```

### 3. Новые сообщения автоматически создают тикеты

Когда приходит **первое сообщение** от нового клиента:

1. ✅ Создается чат
2. ✅ Автоматически присваивается `ticketNumber`
3. ✅ Устанавливается `status = 'new'`
4. ✅ Устанавливается `priority = 'medium'`
5. ✅ Тикет сразу появляется в `GET /api/tickets`

---

## 📝 Логирование

### Создание нового тикета

```
✅ Создан новый чат для JID: 77079861373@s.whatsapp.net 
   (Ваш номер: 77717070766@s.whatsapp.net, 
    Организация: 1, 
    Phone ID: 4, 
    ID чата: 297, 
    Тикет #297)
```

### Увеличение счетчика

```
📬 Увеличен счетчик непрочитанных для чата 297
```

---

## 🎯 Примеры использования

### 1. Получить все новые тикеты

```bash
GET /api/tickets?status=new&page=1&limit=20
```

**Результат:** Все чаты со статусом "new" и назначенными номерами тикетов

### 2. Получить тикет по номеру

```bash
GET /api/tickets/42
```

**Результат:** Детальная информация о тикете #42

### 3. Фильтрация по приоритету

```bash
GET /api/tickets?priority=high&page=1&limit=10
```

---

## ⚙️ Схема базы данных

### Изменения в модели Chat

```prisma
model Chat {
  // ... существующие поля ...
  
  ticketNumber         Int?      @unique  // Автогенерируется!
  status               String    @default("new")
  priority             String    @default("medium")
  
  // ... остальные поля тикет-системы ...
  
  @@unique([organizationId, ticketNumber])
}
```

**Важно:**
- `ticketNumber` уникален в пределах `(organizationId, ticketNumber)`
- Автоматически инкрементируется при создании нового чата
- `NULL` значения допустимы, но не должны встречаться после миграции

---

## 🔄 Жизненный цикл тикета

```
[Первое сообщение от клиента]
          ↓
[Создается чат с ticketNumber]
          ↓
[status = 'new', priority = 'medium']
          ↓
[Тикет появляется в GET /api/tickets]
          ↓
[Оператор может изменить статус/приоритет]
          ↓
[new → open → in_progress → resolved → closed]
```

---

## 🐛 Troubleshooting

### Проблема: Тикеты не появляются в списке

**Причина:** У чатов нет `ticketNumber`

**Решение:**
```bash
node scripts/migrate-ticket-numbers.js
```

### Проблема: Дублирование номеров тикетов

**Причина:** Гонка при одновременном создании нескольких чатов

**Решение:** Использовать транзакции или advisory locks (планируется в будущем)

**Текущее поведение:** Очень редкий случай, т.к. создание чатов происходит последовательно

### Проблема: Номера тикетов пропускают значения

**Причина:** Нормальное поведение при удалении чатов или ошибках создания

**Решение:** Не требуется, пропуски допустимы

---

## 📚 Связанные изменения

### Файлы, затронутые изменениями:

1. **src/config/baileys.ts** - Автогенерация при создании чата
2. **scripts/migrate-ticket-numbers.js** - Миграция существующих чатов
3. **MESSAGE_FLOW_DOCUMENTATION.md** - Обновлена документация

### TODO (будущие улучшения):

- [ ] Настраиваемый формат номеров тикетов (префикс, суффикс)
- [ ] Использование транзакций для предотвращения гонок
- [ ] Автоматическая миграция при старте сервера (опционально)
- [ ] API endpoint для ручного пересчета номеров
- [ ] Логирование создания тикетов в TicketHistory

---

## ✅ Результаты

### Было:
- ❌ Новые сообщения не создавали тикеты
- ❌ API `/api/tickets` возвращал пустой массив
- ❌ 296 чатов без `ticketNumber`

### Стало:
- ✅ Автоматическое создание тикетов для новых чатов
- ✅ API `/api/tickets` возвращает все 296 тикетов
- ✅ Каждый чат имеет уникальный `ticketNumber`
- ✅ Статус и приоритет устанавливаются автоматически

---

## 🎓 Заключение

Система тикетов теперь полностью функциональна:

1. ✅ **Автогенерация номеров** - каждый новый чат получает номер тикета
2. ✅ **Миграция завершена** - все существующие чаты имеют номера
3. ✅ **API работает** - тикеты доступны через REST API
4. ✅ **Интеграция с чатами** - прозрачная работа с входящими сообщениями

**Следующие шаги:**
- Реализация автоназначения тикетов операторам
- SLA мониторинг (firstResponseAt, resolvedAt)
- Уведомления о новых тикетах
- Фронтенд интерфейс для работы с тикетами
