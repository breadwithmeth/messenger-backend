# 🚀 Быстрый старт: Управление сессиями WhatsApp v2.0

## 📚 Документация

| Документ | Описание | Для кого |
|----------|----------|----------|
| [SESSION_ERRORS_QUICK_FIX.md](./SESSION_ERRORS_QUICK_FIX.md) | 🆘 Быстрые решения при ошибках | Операторы |
| [BAD_DECRYPT_FIX.md](./BAD_DECRYPT_FIX.md) | 📖 Полная техническая документация | Разработчики |
| [SESSION_MANAGEMENT_API.md](./SESSION_MANAGEMENT_API.md) | 🔌 API и примеры кода | Разработчики |
| [SESSION_ERRORS_FIX_SUMMARY.md](./SESSION_ERRORS_FIX_SUMMARY.md) | 📊 Сводка изменений v1.0 | Все |
| [SESSION_MANAGEMENT_V2_SUMMARY.md](./SESSION_MANAGEMENT_V2_SUMMARY.md) | ✨ Что нового в v2.0 | Все |

---

## ⚡ Что умеет система

### Автоматически
- ✅ Обнаруживает ошибки шифрования (Bad MAC, Bad Decrypt)
- ✅ Очищает поврежденные данные
- ✅ Восстанавливает сессию без перезапуска
- ✅ Закрывает сессию при критических ошибках
- ✅ Обновляет статус в БД
- ✅ Освобождает память и ресурсы

### Через API
- ✅ Мониторинг здоровья сессий
- ✅ Принудительное закрытие сессии
- ✅ Статистика ошибок в реальном времени

---

## 🎯 Лимиты ошибок

| Тип ошибки | Лимит | Действие при превышении |
|------------|-------|-------------------------|
| **Bad MAC** | 3 | Полный выход из сессии, требуется QR |
| **Bad Decrypt** | 5 | Полный выход из сессии, требуется QR |

**До лимита**: Автоматическая очистка поврежденных данных, сессия продолжает работать  
**При лимите**: Корректное закрытие WebSocket, обновление статуса, требуется новый QR

---

## 🔍 Быстрая диагностика

### Проверка логов на ошибки
```bash
pm2 logs messenger-backend --lines 100 | grep -E "(Bad MAC|bad decrypt)"
```

### Счетчик ошибок
```bash
pm2 logs messenger-backend | grep -E "(Bad MAC error #|Bad Decrypt error #)"
```

**Что искать:**
- `Bad MAC error #1` - первая ошибка, норма
- `Bad MAC error #2` - вторая ошибка, внимание
- `Bad MAC error #3` - сессия будет закрыта автоматически

### Проверка статуса в БД
```bash
psql -U postgres -d messenger_db -c "SELECT id, \"phoneJid\", status FROM \"OrganizationPhone\";"
```

**Статусы:**
- `connected` - все в порядке
- `pending` - ожидание QR-сканирования
- `logged_out` - требуется новое подключение

---

## 🆘 Что делать если...

### Ошибка "Bad MAC" появилась 1-2 раза
**Действие:** Ничего, система сама очистит поврежденные ключи

### Ошибка "Bad MAC" появилась 3 раза
**Действие:** 
1. Система автоматически закроет сессию
2. Статус изменится на `logged_out`
3. Перезапустите сервер: `pm2 restart messenger-backend`
4. Отсканируйте новый QR-код

### Ошибка "bad decrypt" появляется постоянно
**Действие:**
1. Проверьте счетчик в логах
2. При 5 ошибках - автоматическое закрытие
3. Ручная очистка: `node scripts/clean-app-state.js <номер> <orgId>`

### Сессия "зависла"
**Действие:**
```bash
# Проверьте активность в логах
pm2 logs messenger-backend --lines 50

# Если нет активности - принудительное закрытие через БД
psql -U postgres -d messenger_db
DELETE FROM "BaileysAuth" WHERE "phoneJid" = '<номер>';
UPDATE "OrganizationPhone" SET status = 'logged_out' WHERE "phoneJid" LIKE '<номер>%';
\q

pm2 restart messenger-backend
```

---

## 💻 Использование API (для разработчиков)

### Проверка здоровья сессии
```typescript
import { getSessionErrorStats } from './config/baileys';

const stats = getSessionErrorStats(phoneId);

if (!stats.isHealthy) {
  console.warn('Сессия близка к автоматическому закрытию!');
  console.warn(`Bad MAC: ${stats.badMacErrors}/${stats.maxBadMacErrors}`);
  console.warn(`Bad Decrypt: ${stats.badDecryptErrors}/${stats.maxBadDecryptErrors}`);
}
```

### Принудительное закрытие
```typescript
import { forceCloseSession } from './config/baileys';

await forceCloseSession(phoneId, 'Maintenance restart');
```

**Подробнее:** См. [SESSION_MANAGEMENT_API.md](./SESSION_MANAGEMENT_API.md)

---

## 📊 Мониторинг

### Команды для проверки
```bash
# Количество ключей авторизации
psql -U postgres -d messenger_db -c "SELECT COUNT(*) FROM \"BaileysAuth\" WHERE \"phoneJid\" = '<номер>';"

# Типы ключей (для диагностики)
psql -U postgres -d messenger_db -c "SELECT SUBSTRING(\"key\", 1, 20) as type, COUNT(*) FROM \"BaileysAuth\" WHERE \"phoneJid\" = '<номер>' GROUP BY type ORDER BY COUNT(*) DESC;"

# Все активные сессии
psql -U postgres -d messenger_db -c "SELECT \"phoneJid\", status, \"lastConnectedAt\" FROM \"OrganizationPhone\" WHERE status = 'connected';"
```

### Логи в реальном времени
```bash
# Все логи
pm2 logs messenger-backend

# Только ошибки
pm2 logs messenger-backend --err

# С фильтром
pm2 logs messenger-backend | grep -i "error\|warning"
```

---

## 🎓 Обучающие материалы

### Для операторов
1. Прочитайте [SESSION_ERRORS_QUICK_FIX.md](./SESSION_ERRORS_QUICK_FIX.md)
2. Запомните команды проверки логов
3. Знайте когда нужно пересканировать QR

### Для разработчиков
1. Изучите [BAD_DECRYPT_FIX.md](./BAD_DECRYPT_FIX.md) - понимание проблем
2. Прочитайте [SESSION_MANAGEMENT_API.md](./SESSION_MANAGEMENT_API.md) - использование API
3. Ознакомьтесь с [SESSION_MANAGEMENT_V2_SUMMARY.md](./SESSION_MANAGEMENT_V2_SUMMARY.md) - архитектура

### Для DevOps
1. Настройте мониторинг через API
2. Добавьте alerts на критические ошибки
3. Автоматизируйте сбор метрик

---

## ✅ Чеклист проверки

После внедрения проверьте:

- [ ] Логи не содержат критических ошибок
- [ ] Счетчики ошибок сбрасываются при успешном подключении
- [ ] WebSocket закрывается корректно (нет зависших соединений)
- [ ] Статус в БД обновляется правильно
- [ ] QR-код появляется после автоматического закрытия
- [ ] Память не растет со временем (нет утечек)
- [ ] API `getSessionErrorStats()` возвращает корректные данные
- [ ] Принудительное закрытие через `forceCloseSession()` работает

---

## 🐛 Известные проблемы

### Нет проблем! 🎉

Все известные проблемы решены в версии 2.0.

Если найдете баг - создайте issue или обратитесь к разработчикам.

---

## 📞 Поддержка

**Нашли проблему?**
1. Проверьте логи: `pm2 logs messenger-backend`
2. Проверьте БД: статус телефонов и количество ключей
3. Посмотрите в [SESSION_ERRORS_QUICK_FIX.md](./SESSION_ERRORS_QUICK_FIX.md)
4. Если не помогло - обратитесь к команде разработки

---

**Версия**: 2.0  
**Обновлено**: 22 ноября 2025  
**Статус**: Production Ready ✅
