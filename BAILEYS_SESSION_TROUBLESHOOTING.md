# 🔍 Анализ проблемы завершения сессий WhatsApp

## 📊 Информация об обновлении

- **Текущая версия Baileys**: 6.7.21
- **Предыдущая версия**: 6.7.20
- **Дата обновления**: Январь 2025

---

## ❌ Проблема: Частое завершение сессий

### Наблюдаемые симптомы:

1. **Ошибка 401 (device_removed)**
   - WhatsApp отключает устройство
   - Сессия завершается без возможности переподключения
   - Требуется повторное сканирование QR-кода

2. **Ошибка 408 (QR timeout)**
   - QR-код не был отсканирован в течение ~2 минут
   - Требуется генерация нового QR-кода

---

## 🔎 Возможные причины завершения сессий

### 1. **Множественное сканирование QR-кода**

**Описание**: Если один и тот же QR-код сканируется несколько раз, WhatsApp может расценить это как подозрительную активность.

**Признаки**:
- Ошибка `device_removed` (401) появляется сразу после подключения
- В логах видно несколько попыток подключения для одного номера

**Решение**:
- Сканировать QR-код ТОЛЬКО ОДИН РАЗ для каждого номера
- Не обновлять страницу с QR-кодом
- Не пытаться подключить один номер к нескольким серверам

### 2. **Проблемы с keepalive**

**Описание**: WhatsApp требует регулярной отправки пакетов keepalive для поддержания соединения.

**Признаки**:
- Сессия завершается через несколько минут/часов бездействия
- Нет явной ошибки, просто `connection: 'close'`

**Текущая конфигурация**:
```typescript
const sock = makeWASocket({
  auth: state,
  printQRInTerminal: false,
  logger: pino({ level: 'silent' }),
  browser: ['Chrome (Linux)', '', ''],
  keepAliveIntervalMs: 30000, // 30 секунд
});
```

**Рекомендации**:
- ✅ Keepalive уже настроен (30 секунд)
- Можно попробовать уменьшить до 20-25 секунд
- Убедиться, что сервер не перезагружается

### 3. **Нестабильное интернет-соединение сервера**

**Описание**: Если сервер теряет соединение с интернетом, WhatsApp разрывает сессию.

**Признаки**:
- Случайные отключения без явной причины
- Ошибки в логах о недоступности сети

**Проверка**:
```bash
# Проверить стабильность соединения
ping -c 100 8.8.8.8

# Проверить DNS
nslookup web.whatsapp.com
```

**Решение**:
- Убедиться в стабильности интернет-соединения
- Использовать проводное подключение вместо WiFi
- Настроить автоматическое переподключение (уже реализовано)

### 4. **Проблемы с базой данных аутентификации**

**Описание**: Поврежденные или несовместимые данные авторизации могут вызывать отключения.

**Признаки**:
- Ошибки при чтении/записи auth данных
- `device_removed` после успешного подключения

**Проверка**:
```sql
-- Проверить состояние auth данных
SELECT "organizationId", "phoneJid", "key", 
       LENGTH("value") as value_length,
       "createdAt", "updatedAt"
FROM "BaileysAuth"
ORDER BY "updatedAt" DESC
LIMIT 20;
```

**Решение**:
- Удалить старые auth данные для проблемных номеров
- Перегенерировать QR-код
- Убедиться, что база данных не переполнена

### 5. **Ограничения WhatsApp Business API**

**Описание**: WhatsApp может ограничивать количество подключенных устройств или активность.

**Признаки**:
- Отключения происходят при высокой нагрузке
- Несколько номеров одновременно получают `device_removed`

**Рекомендации**:
- Не подключать более 4-5 устройств на один аккаунт WhatsApp
- Соблюдать rate limits на отправку сообщений
- Избегать массовых рассылок

### 6. **Bugs в Baileys 6.7.20**

**Описание**: Версия 6.7.20 могла содержать баги, связанные с управлением сессиями.

**Что сделано**:
- ✅ Обновлено до версии 6.7.21
- ✅ Установлены все зависимости

**Changelog 6.7.21** (ожидается проверка):
- Возможные исправления в обработке auth данных
- Улучшения в reconnection logic
- Патчи безопасности

---

## 🛠️ Рекомендации по диагностике

### Шаг 1: Проверить текущее состояние сессий

```sql
-- Состояние всех номеров в организации
SELECT 
    id,
    "phoneNumber",
    "phoneJid",
    status,
    "lastConnectedAt",
    "qrCode" IS NOT NULL as has_qr,
    "createdAt"
FROM "OrganizationPhone"
ORDER BY "lastConnectedAt" DESC;
```

### Шаг 2: Проверить логи при подключении

Запустить сервер с подробными логами:

```bash
# В src/config/baileys.ts изменить уровень логирования
logger: pino({ level: 'debug' }) // вместо 'silent'
```

Искать в логах:
- `[ConnectionUpdate]` - события подключения
- `[Connection] Соединение закрыто` - причины отключения
- `device_removed`, `401`, `408` - коды ошибок

### Шаг 3: Мониторинг после обновления

После перезапуска с Baileys 6.7.21 отслеживать:

1. **Частоту отключений**:
   - Было: каждые X минут/часов
   - Стало: ?

2. **Типы ошибок**:
   - Было: `device_removed` (401)
   - Стало: ?

3. **Стабильность QR-кодов**:
   - Успешно ли проходит сканирование
   - Как долго держится соединение после сканирования

### Шаг 4: Тестирование с одним номером

Рекомендуется:
1. Оставить подключенным только ОДИН тестовый номер
2. Не отправлять сообщения (только получать)
3. Наблюдать за стабильностью в течение 24 часов
4. Если стабильно - подключать следующие номера по одному

---

## ✅ Что уже сделано правильно

### 1. Автоматическое переподключение

```typescript
if (connection === 'close') {
  const shouldReconnect =
    (lastDisconnect?.error as Boom)?.output?.statusCode !== DisconnectReason.loggedOut;
  
  if (shouldReconnect) {
    await new Promise(resolve => setTimeout(resolve, 3000));
    logger.info(`[Connection] Попытка переподключения для ${phoneJid}...`);
    startBaileys(organizationId, organizationPhoneId, phoneJid);
  }
}
```

✅ **Правильно**: Автоматически переподключается при разрыве соединения (кроме logout)

### 2. Очистка данных при logout

```typescript
if (!shouldReconnect) {
  const key = phoneJid.split('@')[0].split(':')[0];
  
  await prisma.baileysAuth.deleteMany({
    where: {
      organizationId: organizationId,
      phoneJid: key,
    }
  });
  
  await prisma.organizationPhone.update({
    where: { id: organizationPhoneId },
    data: { status: 'logged_out', qrCode: null },
  });
}
```

✅ **Правильно**: Очищает старые auth данные при полном logout

### 3. Хранение сессий в БД

```typescript
const { state, saveCreds, clearAll } = await useDBAuthState(
  organizationId,
  phoneJid,
  prisma,
  logger
);
```

✅ **Правильно**: Сессии сохраняются в PostgreSQL, переживают перезапуск сервера

### 4. Keepalive настроен

```typescript
keepAliveIntervalMs: 30000, // 30 секунд
```

✅ **Правильно**: Регулярные пакеты keepalive для поддержания соединения

---

## 🔧 Дополнительные улучшения (опционально)

### 1. Более агрессивное переподключение

```typescript
// Текущее значение: 3000 мс (3 секунды)
await new Promise(resolve => setTimeout(resolve, 3000));

// Можно попробовать:
await new Promise(resolve => setTimeout(resolve, 5000)); // 5 секунд
```

**Причина**: Дать WhatsApp больше времени перед новой попыткой подключения

### 2. Экспоненциальная задержка (backoff)

```typescript
let reconnectAttempts = 0;
const MAX_ATTEMPTS = 10;

if (shouldReconnect && reconnectAttempts < MAX_ATTEMPTS) {
  const delay = Math.min(1000 * Math.pow(2, reconnectAttempts), 60000);
  reconnectAttempts++;
  
  await new Promise(resolve => setTimeout(resolve, delay));
  logger.info(`[Connection] Попытка #${reconnectAttempts} переподключения для ${phoneJid}...`);
  startBaileys(organizationId, organizationPhoneId, phoneJid);
}
```

**Причина**: Избежать быстрых повторных попыток, которые могут восприниматься как атака

### 3. Мониторинг здоровья соединения

```typescript
// Добавить в connection.update
if (connection === 'open') {
  // Запустить периодическую проверку
  const healthCheckInterval = setInterval(async () => {
    const sock = socks.get(organizationPhoneId);
    if (!sock || sock.ws?.readyState !== 1) { // 1 = OPEN
      logger.warn(`⚠️ Сокет для ${phoneJid} не в состоянии OPEN. ReadyState: ${sock?.ws?.readyState}`);
      clearInterval(healthCheckInterval);
    } else {
      logger.debug(`✅ Сокет для ${phoneJid} работает нормально`);
    }
  }, 60000); // Каждую минуту
}
```

### 4. Логирование метрик

```typescript
// Создать таблицу для метрик
CREATE TABLE "ConnectionMetrics" (
  "id" SERIAL PRIMARY KEY,
  "organizationPhoneId" INTEGER NOT NULL,
  "event" TEXT NOT NULL, -- 'connected', 'disconnected', 'qr_generated'
  "errorCode" INTEGER,
  "errorMessage" TEXT,
  "timestamp" TIMESTAMP NOT NULL DEFAULT NOW()
);

// Логировать события
await prisma.connectionMetrics.create({
  data: {
    organizationPhoneId,
    event: 'connected',
    timestamp: new Date(),
  }
});
```

---

## 📋 План действий

### Немедленные действия (сейчас):

1. ✅ **Обновить Baileys до 6.7.21** - ВЫПОЛНЕНО
2. ⏳ **Пересобрать проект**:
   ```bash
   npm run build
   ```
3. ⏳ **Перезапустить сервер**:
   ```bash
   npm start
   ```

### Краткосрочные действия (сегодня):

4. ⏳ **Очистить все старые сессии**:
   ```bash
   # Удалить все auth данные
   npm run prisma:studio
   # Вручную удалить записи из BaileysAuth
   
   # ИЛИ через SQL:
   # TRUNCATE TABLE "BaileysAuth";
   ```

5. ⏳ **Подключить ОДИН номер**:
   - Сгенерировать QR-код
   - Отсканировать
   - Наблюдать в течение 2-4 часов

6. ⏳ **Проверить логи**:
   - Искать `device_removed`
   - Искать `connection: 'close'`
   - Записать время отключений (если будут)

### Среднесрочные действия (эта неделя):

7. ⏳ **Постепенно добавлять номера**:
   - Один номер в день
   - Наблюдать за стабильностью

8. ⏳ **Мониторинг метрик**:
   - Создать таблицу ConnectionMetrics
   - Логировать все события подключения/отключения

9. ⏳ **Документировать паттерны**:
   - Когда происходят отключения (время суток?)
   - Какие номера затронуты
   - Предшествующие события (много сообщений? отправка медиа?)

---

## 🧪 Тестовый сценарий

### Сценарий 1: Стабильность простого подключения

1. Очистить все auth данные
2. Подключить один номер (77054810862)
3. НЕ отправлять сообщения
4. Только ПОЛУЧАТЬ сообщения (попросить кого-то написать 2-3 сообщения)
5. Наблюдать 24 часа

**Ожидаемый результат**: Соединение стабильно, нет отключений

**Если отключается**: Проблема в Baileys или WhatsApp API, не в вашем коде

### Сценарий 2: Стабильность при отправке

1. После успешного Сценария 1
2. Отправить 5-10 сообщений через API
3. Наблюдать следующие 2-4 часа

**Ожидаемый результат**: Соединение стабильно после отправки

**Если отключается**: Проблема в rate limiting или логике отправки

### Сценарий 3: Множественные номера

1. После успешных Сценариев 1-2
2. Подключить второй номер
3. Наблюдать 24 часа

**Ожидаемый результат**: Оба соединения стабильны

**Если отключается**: Проблема в управлении множественными сессиями

---

## 📝 Что записывать в логах

При каждом отключении фиксировать:

```
=== DISCONNECT EVENT ===
Время: <timestamp>
Номер: <phoneJid>
Код ошибки: <statusCode>
Сообщение ошибки: <error message>
Время с последнего подключения: <duration>
Последнее действие: <last API call / message sent/received>
Версия Baileys: 6.7.21
========================
```

Это поможет найти паттерн в отключениях.

---

## 🎯 Ожидаемые результаты после обновления

### Если Baileys 6.7.21 исправил проблему:

- ✅ Соединения стабильны 24+ часов
- ✅ Нет `device_removed` (401) ошибок
- ✅ QR-коды сканируются успешно с первого раза
- ✅ Автоматическое переподключение работает при временных сбоях

### Если проблема остается:

- ❌ Продолжаются отключения с теми же ошибками
- 🔍 Требуется более глубокое исследование:
  - Анализ сетевого трафика
  - Проверка серверных ограничений WhatsApp
  - Обращение в сообщество Baileys (GitHub Issues)
  - Рассмотрение альтернативных решений (WhatsApp Business API)

---

## 🔗 Полезные ссылки

- [Baileys GitHub](https://github.com/WhiskeySockets/Baileys)
- [Baileys Issues](https://github.com/WhiskeySockets/Baileys/issues)
- [Changelog 6.7.21](https://github.com/WhiskeySockets/Baileys/releases)
- [WhatsApp Business API Limits](https://developers.facebook.com/docs/whatsapp/api/rate-limits)

---

## 📞 Следующие шаги

1. **Пересобрать и перезапустить сервер** с новой версией Baileys
2. **Очистить старые auth данные** и подключить номера заново
3. **Мониторить логи** в течение 24-48 часов
4. **Документировать результаты** - стало лучше или нет
5. **Сообщить о результатах** для дальнейшего анализа

---

**Дата создания документа**: Январь 2025  
**Версия Baileys**: 6.7.21  
**Статус**: Ожидание результатов после обновления
