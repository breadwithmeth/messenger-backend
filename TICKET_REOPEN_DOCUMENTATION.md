# 🔄 Переоткрытие тикетов при новых сообщениях

## Описание функции

Когда в закрытый тикет приходит новое сообщение от клиента, система автоматически создает **новый тикет** с новым номером вместо того, чтобы открывать старый.

## Зачем это нужно? 🎯

### Проблема

Клиент написал вам, вы решили его вопрос и закрыли тикет (`status = 'closed'`). Через некоторое время клиент пишет снова с новым вопросом.

### Варианты поведения

#### ❌ **Плохой подход**: Открыть старый тикет
- История старого и нового вопроса смешивается
- Непонятно, сколько раз клиент обращался
- Невозможно вести статистику по количеству обращений
- Путаница с временем открытия/закрытия

#### ✅ **Правильный подход**: Создать новый тикет
- Каждое обращение = отдельный тикет
- Чистая история по каждому вопросу
- Корректная статистика обращений
- SLA считается для каждого обращения отдельно

## Как это работает 🔧

### Логика в `ensureChat()`

```typescript
// При получении сообщения проверяем существующий чат
if (chat) {
  // Если чат был закрыт
  if (chat.status === 'closed') {
    // Генерируем новый номер тикета
    const lastTicket = await prisma.chat.findFirst({
      where: { 
        organizationId,
        ticketNumber: { not: null }
      },
      orderBy: { ticketNumber: 'desc' },
    });
    
    const nextTicketNumber = (lastTicket?.ticketNumber || 0) + 1;
    
    // Создаем новый тикет
    updateData.ticketNumber = nextTicketNumber;
    updateData.status = 'new';
    updateData.priority = 'medium';
    updateData.assignedTo = null; // Сбрасываем назначение
    updateData.closedAt = null;
  }
}
```

### Что происходит при получении нового сообщения

```
1. Приходит сообщение от клиента
   ↓
2. ensureChat() ищет существующий чат по JID
   ↓
3. Чат найден → проверяем статус
   ↓
4. Статус = 'closed'?
   ├─ ДА → Создаем новый тикет:
   │       • ticketNumber = следующий номер (например, 297 → 298)
   │       • status = 'new'
   │       • priority = 'medium'
   │       • assignedTo = null (убираем назначение)
   │       • closedAt = null
   │       • Лог: "Чат #123 был закрыт - создан новый тикет #298"
   │
   └─ НЕТ → Просто обновляем lastMessageAt
```

## Примеры использования 📝

### Сценарий 1: Первое обращение клиента

```
Клиент (77079861373): "Здравствуйте, у меня вопрос"
→ Создается чат #123
→ Создается тикет #297
→ status = 'new', priority = 'medium'

Оператор: "Здравствуйте! Чем могу помочь?"
→ status = 'open' (оператор взял в работу)

Клиент: "Спасибо, все понятно!"
Оператор закрывает тикет
→ status = 'closed', closedAt = "2025-11-16T10:30:00Z"
```

### Сценарий 2: Повторное обращение (через день)

```
Клиент (77079861373): "Добрый день, еще вопрос"
→ Найден существующий чат #123 (status = 'closed')
→ Создается НОВЫЙ тикет #298 для того же чата
→ status меняется: 'closed' → 'new'
→ priority = 'medium'
→ assignedTo = null (сбрасывается)
→ closedAt = null

ЛОГ: "🔄 Чат #123 был закрыт - создан новый тикет #298 (статус изменен: closed → new)"
```

### Сценарий 3: Сообщение в активный тикет

```
Клиент (77079861373): "Еще информация по текущему вопросу"
→ Найден существующий чат #123 (status = 'open')
→ Новый тикет НЕ создается
→ Просто обновляется lastMessageAt
→ Сообщение добавляется в текущий тикет #298
```

## История тикетов по одному чату 📊

Один чат может иметь множество тикетов в истории:

```sql
-- Найти все тикеты клиента 77079861373
SELECT 
  ticketNumber,
  status,
  createdAt,
  closedAt,
  EXTRACT(EPOCH FROM (closedAt - createdAt))/3600 AS hours_to_resolve
FROM "Chat"
WHERE remoteJid = '77079861373@s.whatsapp.net'
  AND organizationId = 1
ORDER BY createdAt DESC;
```

Пример результата:

| ticketNumber | status | createdAt           | closedAt            | hours_to_resolve |
|--------------|--------|---------------------|---------------------|------------------|
| 298          | open   | 2025-11-17 09:00:00 | NULL                | NULL             |
| 297          | closed | 2025-11-16 08:00:00 | 2025-11-16 10:30:00 | 2.5              |
| 256          | closed | 2025-11-10 14:00:00 | 2025-11-10 15:00:00 | 1.0              |

Видно, что:
- Клиент обращался 3 раза
- Первые 2 обращения решены (закрыты)
- Третье обращение (тикет #298) сейчас в работе

## База данных 🗄️

### Структура Chat

```prisma
model Chat {
  id                    Int       @id @default(autoincrement())
  organizationId        Int
  remoteJid             String    // JID клиента (неизменный)
  ticketNumber          Int?      // Меняется при переоткрытии!
  status                String?   // 'new', 'open', 'closed'
  priority              String?   // 'low', 'medium', 'high'
  assignedTo            Int?      // Сбрасывается при переоткрытии
  closedAt              DateTime? // Сбрасывается при переоткрытии
  createdAt             DateTime
  lastMessageAt         DateTime
  
  @@unique([organizationId, ticketNumber])
  @@unique([organizationId, receivingPhoneJid, remoteJid])
}
```

### Важно! ⚠️

**Один чат (по JID) = много тикетов в истории**

- `remoteJid` - **постоянный**, идентифицирует клиента
- `ticketNumber` - **меняется** каждый раз при переоткрытии
- История сохраняется через `createdAt` и `closedAt`

### Как хранится история

При создании нового тикета для закрытого чата:

```
БЫЛО:
Chat #123:
  remoteJid = "77079861373@s.whatsapp.net"
  ticketNumber = 297
  status = "closed"
  closedAt = "2025-11-16T10:30:00Z"

СТАЛО (после нового сообщения):
Chat #123:
  remoteJid = "77079861373@s.whatsapp.net"  ← не меняется
  ticketNumber = 298                          ← ИЗМЕНИЛСЯ!
  status = "new"                              ← открыт заново
  closedAt = null                             ← сброшен
  assignedTo = null                           ← сброшен
```

## Логи системы 📝

### При создании нового тикета для закрытого чата

```
🔄 Чат #123 был закрыт - создан новый тикет #298 (статус изменен: closed → new)
📬 Увеличен счетчик непрочитанных для чата 123
💾 Сообщение (тип: text, ID: 5679) сохранено в БД
```

### При обычном сообщении в активный чат

```
📬 Увеличен счетчик непрочитанных для чата 123
💾 Сообщение (тип: text, ID: 5680) сохранено в БД
```

## API Endpoints 🌐

### Получить текущий тикет клиента

```bash
curl "http://localhost:4000/api/tickets?search=77079861373" \
  -H "Authorization: Bearer YOUR_TOKEN"
```

Вернет **текущий** тикет (последний созданный).

### Получить историю всех тикетов

**Вариант 1:** Через API чата (если такой эндпоинт есть)

```bash
curl "http://localhost:4000/api/chats/123/tickets" \
  -H "Authorization: Bearer YOUR_TOKEN"
```

**Вариант 2:** Напрямую из БД

```sql
SELECT * FROM "Chat" 
WHERE remoteJid = '77079861373@s.whatsapp.net'
  AND organizationId = 1
ORDER BY createdAt DESC;
```

## Статистика 📊

### Количество обращений клиента

```sql
SELECT 
  remoteJid,
  COUNT(*) as total_tickets,
  SUM(CASE WHEN status = 'closed' THEN 1 ELSE 0 END) as resolved_tickets,
  SUM(CASE WHEN status != 'closed' THEN 1 ELSE 0 END) as active_tickets
FROM "Chat"
WHERE organizationId = 1
GROUP BY remoteJid
HAVING COUNT(*) > 1
ORDER BY total_tickets DESC;
```

### Среднее время решения

```sql
SELECT 
  AVG(EXTRACT(EPOCH FROM (closedAt - createdAt))/3600) as avg_hours_to_resolve
FROM "Chat"
WHERE status = 'closed'
  AND closedAt IS NOT NULL
  AND organizationId = 1;
```

## Бизнес-логика 💼

### Автоназначение при переоткрытии

**Вопрос:** Назначить тикет тому же оператору, что работал с предыдущим?

**Ответ:** НЕТ, потому что:
- Оператор может быть уже недоступен
- Нагрузка должна распределяться равномерно
- Новое обращение = новый вопрос

**Решение:** При переоткрытии `assignedTo = null`, система назначит по round-robin.

### Приоритет при переоткрытии

**Вопрос:** Сохранить приоритет предыдущего тикета?

**Ответ:** НЕТ, сбрасываем на `'medium'`, потому что:
- Новое обращение может быть другой важности
- Оператор должен оценить приоритет заново

**Альтернатива:** Можно добавить логику повышения приоритета для частых обращений:

```typescript
// Подсчитать количество закрытых тикетов клиента
const closedTicketsCount = await prisma.chat.count({
  where: {
    remoteJid: normalizedRemoteJid,
    organizationId,
    status: 'closed',
  }
});

// Если клиент обращается часто - повысить приоритет
const priority = closedTicketsCount >= 3 ? 'high' : 'medium';
```

## Уведомления 🔔

### Для операторов

При создании нового тикета из закрытого чата:

```json
{
  "event": "ticket.reopened",
  "data": {
    "chatId": 123,
    "oldTicketNumber": 297,
    "newTicketNumber": 298,
    "remoteJid": "77079861373@s.whatsapp.net",
    "message": "Клиент написал снова после закрытия тикета",
    "previousClosedAt": "2025-11-16T10:30:00Z"
  }
}
```

### Для аналитики

Отслеживать клиентов с частыми обращениями:

```typescript
// После создания нового тикета
if (chat.status === 'closed') {
  // Подсчитать общее количество тикетов клиента
  const totalTickets = await prisma.chat.count({
    where: {
      remoteJid: normalizedRemoteJid,
      organizationId,
    }
  });
  
  // Если клиент часто обращается - уведомить менеджера
  if (totalTickets >= 5) {
    notificationService.send({
      type: 'frequent_customer',
      customerId: remoteJid,
      totalTickets: totalTickets,
      message: `Клиент обратился ${totalTickets} раз`,
    });
  }
}
```

## Тестирование 🧪

### Сценарий 1: Создание и закрытие тикета

```bash
# 1. Отправить сообщение клиенту (создаст новый тикет)
# 2. Проверить тикет
curl "http://localhost:4000/api/tickets" | jq '.tickets[0]'

# 3. Закрыть тикет
curl -X PATCH "http://localhost:4000/api/tickets/298/status" \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"status": "closed"}'

# 4. Убедиться, что тикет закрыт
curl "http://localhost:4000/api/tickets/298" | jq '.status'
# Ожидаемый результат: "closed"
```

### Сценарий 2: Новое сообщение в закрытый тикет

```bash
# 1. Клиент отправляет новое сообщение в WhatsApp
# 2. Проверить логи сервера
# Ожидаемый лог: "🔄 Чат #123 был закрыт - создан новый тикет #299"

# 3. Проверить, что создан новый тикет
curl "http://localhost:4000/api/tickets?search=77079861373" | jq '.tickets[0].ticketNumber'
# Ожидаемый результат: 299

# 4. Проверить статус
curl "http://localhost:4000/api/tickets/299" | jq '.status'
# Ожидаемый результат: "new"
```

### Сценарий 3: Проверка сброса полей

```bash
# После переоткрытия проверить, что поля сброшены
curl "http://localhost:4000/api/tickets/299" | jq '{assignedTo, closedAt, priority, status}'

# Ожидаемый результат:
# {
#   "assignedTo": null,
#   "closedAt": null,
#   "priority": "medium",
#   "status": "new"
# }
```

## Миграция данных 🔄

Эта функция работает только для **новых** сообщений. Существующие закрытые чаты не изменяются.

Если нужно "переоткрыть" закрытые чаты вручную:

```sql
-- НЕ ДЕЛАЙТЕ ТАК! Это только для примера
UPDATE "Chat"
SET 
  status = 'new',
  closedAt = NULL,
  assignedTo = NULL
WHERE status = 'closed'
  AND organizationId = 1;
```

**Лучше:** Дождитесь нового сообщения, система сама создаст новый тикет.

## Будущие улучшения 🚀

### 1. Связь между тикетами

Добавить поле `previousTicketId` для отслеживания истории:

```prisma
model Chat {
  // ...
  previousTicketId Int? // ID предыдущего тикета этого же чата
  previousTicket   Chat? @relation("TicketHistory", fields: [previousTicketId], references: [id])
  nextTickets      Chat[] @relation("TicketHistory")
}
```

### 2. Причина переоткрытия

```prisma
model Chat {
  // ...
  reopenReason String? // "new_message", "manual", "escalation"
  reopenedAt   DateTime?
}
```

### 3. Настройки по организациям

```typescript
// Разрешить организациям выбирать поведение
organizationSettings: {
  ticketReopenBehavior: 'create_new' | 'reopen_existing'
}
```

### 4. Счетчик переоткрытий

```prisma
model Chat {
  // ...
  reopenCount Int @default(0) // Сколько раз клиент писал после закрытия
}
```

## Заключение ✨

Функция автоматического создания нового тикета при сообщении в закрытый чат:

✅ **Преимущества:**
- Четкая история обращений клиента
- Корректная статистика по тикетам
- Раздельный учет SLA для каждого обращения
- Чистые данные для аналитики

✅ **Работает автоматически:**
- Оператору не нужно ничего делать
- Клиент просто пишет сообщение
- Система создает новый тикет с новым номером

✅ **Сохраняет контекст:**
- История сообщений в чате сохраняется
- Можно отследить, что клиент обращался ранее
- Можно построить граф связей между тикетами

---

**Система готова к работе с повторными обращениями! 🎉**
