# Обработка реплаев (ответов на сообщения)

**Дата изменения:** 9 января 2026  
**Файлы:** 
- `src/config/baileys.ts` (Baileys WhatsApp)
- `src/controllers/wabaController.ts` (WhatsApp Business API)
- `src/controllers/chatController.ts` (API для получения сообщений)

## Описание

Реализована система обработки реплаев (ответов на сообщения) для WhatsApp через Baileys и WhatsApp Business API (WABA). Информация об ответе сохраняется как часть основного текста сообщения, а также в отдельных полях для возможности структурированного отображения на фронтенде.

## Как это работает

### Формат хранения

#### В базе данных
```
message.content = "ответил на: "Оригинальное сообщение"\n\nМой ответ"
message.quotedMessageId = "3EB0..." (опционально)
message.quotedContent = "Оригинальное сообщение" (опционально)
```

### API возвращает
При получении сообщений через API (`GET /api/chats/:chatId/messages` или `GET /api/waba/operator/chat/:chatId/messages`) возвращаются следующие поля:
- `content` - полный текст с форматированным реплаем
- `quotedMessageId` - ID цитируемого сообщения (если есть)
- `quotedContent` - текст цитируемого сообщения (если есть)

Фронтенд может использовать либо `content` для простого отображения, либо `quotedMessageId`/`quotedContent` для структурированного отображения с UI цитирования.

## Реализация для WhatsApp Business API (WABA)

### 1. Структура реплая в WABA

В WABA реплаи имеют следующую структуру:
```json
{
  "context": {
    "from": "77054810862",
    "id": "wamid.HBgLNzcwNTQ4MTA4NjIVAgASGBQzQjc5QUZDNDE5QzdGNDBDNDBCQwA="
  },
  "from": "77054810862",
  "id": "wamid.HBgLNzcwNTQ4MTA4NjIVAgASGBQzQjAyM0FENkU5NzAzM0Q4RTZGQwA=",
  "timestamp": "1767989655",
  "text": {
    "body": "тест2"
  },
  "type": "text"
}
```

Где:
- `context.id` - ID цитируемого сообщения (wamid)
- `context.from` - номер отправителя цитируемого сообщения

### 2. Обработка входящих реплаев

Когда сообщение содержит `message.context.id`, код:

1. **Извлекает ID цитируемого сообщения**:
   ```typescript
   quotedMessageId = message.context.id;
   ```

2. **Находит цитируемое сообщение в базе данных**:
   ```typescript
   const quotedDbMsg = await prisma.message.findFirst({
     where: {
       whatsappMessageId: quotedMessageId,
       organizationPhoneId: orgPhone.id,
     },
     select: { content: true, type: true, mediaUrl: true },
   });
   ```

3. **Извлекает текст в зависимости от типа сообщения**:
3. **Извлекает текст в зависимости от типа сообщения**:
   - `text` - текст сообщения
   - `image` - подпись или `[Изображение]`
   - `video` - подпись или `[Видео]`
   - `document` - подпись или `[Документ]`
   - `audio` - `[Аудио]`
   - Другие типы - `[тип сообщения]`

4. **Форматирует текст ответа**:
   ```typescript
   const replyText = `ответил на: "${quotedContent}"`;
   content = `${replyText}\n\n${content}`;
   ```

5. **Сохраняет в БД** все три поля: `content`, `quotedMessageId`, `quotedContent`

### 2. Поддержка реплаев для всех типов сообщений

Реплаи теперь работают не только для текстовых сообщений, но и для:
- 📝 Текстовых сообщений
- 🖼️ Изображений с подписями
- 🎬 Видео с подписями
- 📄 Документов
- 🎵 Аудио
- 🔘 Кнопок и интерактивных элементов

## Реализация для Baileys WhatsApp

### 1. Обработка contextInfo в extendedTextMessage

Аналогично WABA, Baileys обрабатывает `contextInfo.quotedMessage` из входящих сообщений.

## Изменения в API

### Контроллеры

#### chatController.ts
- `getChatMessages()` - теперь возвращает `quotedContent` для каждого сообщения
- `listChats()` - возвращает `quotedContent` для последнего сообщения в чате

#### wabaController.ts  
- `handleIncomingMessage()` - обрабатывает реплаи для всех типов WABA сообщений
- `getChatMessages()` - возвращает `quotedMessageId` и `quotedContent`

## Пример

**Входящее сообщение через WABA:**
```json
{
  "type": "text",
  "text": { "body": "Спасибо, всё хорошо!" },
  "cofrom": "77054810862",
    "id": "wamid.HBgLNzcwNTQ4MTA4NjIVAgASGBQzQjc5QUZDNDE5QzdGNDBDNDBCQwA="
  }
}
```

После поиска в БД по `context.id`:
```json
{
  "whatsappMessageId": "wamid.HBgLNzcwNTQ4MTA4NjIVAgASGBQzQjc5QUZDNDE5QzdGNDBDNDBCQwA=",
  "content": "Как дела?" }
  }
}
```

**Сохранённое в БД:**
```json
{
  "content": "ответил на: \"Как дела?\"\n\nСпасибо, всё хорошо!",
  "quotedMessageId": "wamid.xxx",
  "quotedContent": "Как дела?"
}
```

**Ответ на изображение:**
```json
{
  "type": "image",
  "image": { 
    "id": "media_id",
    "caption": "Вот фото" 
  },
  "context": {
    "quoted_message_id": "wamid.yyy",
    "quoted_message": {
      "text": { "body": "Покажи фото" }
    }
  }
}
```

**Сохранённое в БД:**
```json
{
  "content": "ответил на: \"Покажи фото\"\n\nВот фото",
  "type": "image",
  "mediaUrl": "https://...",
  "quotedMessageId": "wamid.yyy",
  "quotedContent": "Покажи фото"
}
```

## Логирование

При получении ответа логируется:
```
[reply] Ответ на сообщение ID: wamid.xxx, текст: "Как дела?"
```

## Преимущества

✅ Работает для WABA и Baileys  
✅ Поддерживает все типы сообщений (текст, изображения, видео, документы, аудио)  
✅ Гибкое отображение на фронтенде (простое или структурированное)  
✅ Полная информация об ответе доступна через API  
✅ Читаемый формат в поле `content`  
✅ Сохраняет структурированные данные для расширенного UI

## Использование на фронтенде

### Простое отображение
Используйте поле `content` напрямую:
```typescript
<div>{message.content}</div>
```

### Структурированное отображение
Используйте `quotedMessageId` и `quotedContent` для создания UI цитирования:
```typescript
{message.quotedContent && (
  <div className="quoted-message">
    <div className="quoted-indicator">↩️</div>
    <div className="quoted-text">{message.quotedContent}</div>
  </div>
)}
<div className="message-content">
  {message.content.replace(/^ответил на:.*?\n\n/, '')}
</div>
```

## Поддерживаемые типы медиа в ответах

- 📝 Текстовые сообщения
- 🖼️ Изображения (используется caption или `[Изображение]`)
- 🎬 Видео (используется caption или `[Видео]`)
- 📄 Документы (используется caption или имя файла)
- 🎵 Аудио (отмечается как `[Аудио]`)
- 📁 Другие файлы - отмечаются как `[Медиафайл]`

- 🎬 Видео (используется caption или `[Видео]`)
- 📄 Документы (используется caption или имя файла)
- 🎵 Аудио (отмечается как `[Аудио]`)
- 📁 Другие файлы - отмечаются как `[Медиафайл]`
