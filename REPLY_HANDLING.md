# Обработка реплаев (ответов на сообщения)

**Дата изменения:** 7 января 2026  
**Файл:** `src/config/baileys.ts`

## Описание

Реализована новая система обработки реплаев (ответов на сообщения) в Baileys WhatsApp интеграции. Теперь вместо сохранения отдельных полей `quotedMessageId` и `quotedContent`, информация об ответе сохраняется как часть основного текста сообщения.

## Как это работает

### До изменений
```
message.content = "Мой ответ"
message.quotedMessageId = "3EB0..."
message.quotedContent = "Оригинальное сообщение"
```

### После изменений
```
message.content = "ответил на: "Оригинальное сообщение"\n\nМой ответ"
```

## Реализация

### 1. Обработка contextInfo в extendedTextMessage

Когда сообщение содержит `contextInfo.quotedMessage`, код:

1. **Извлекает текст цитируемого сообщения** из доступных полей:
   - `conversation` - обычный текст
   - `extendedTextMessage.text` - расширенный текст
   - `imageMessage.caption` - подпись к изображению
   - `videoMessage.caption` - подпись к видео
   - `documentMessage.fileName` - имя документа
   - `[Медиафайл]` - плейсхолдер для медиа без текста

2. **Форматирует текст ответа**:
   ```typescript
   const replyText = `ответил на: "${quotedContent}"`;
   ```

3. **Объединяет с основным контентом**:
   ```typescript
   content = `${replyText}\n\n${content}`;
   ```

### 2. Сохранение в базе данных

Вся информация сохраняется в одном поле `content`:
- No extra fields needed
- Более читаемый формат
- Совместимо со старой структурой базы данных

## Пример

**Входящее сообщение:**
```
Пользователь ответил на сообщение "Как дела?"
Отправил текст: "Спасибо, всё хорошо!"
```

**Сохранённое в БД:**
```
content: "ответил на: "Как дела?"\n\nСпасибо, всё хорошо!"
```

## Логирование

При получении ответа логируется:
```
[reply] Ответ на сообщение ID: 3EB0ABC123...
```

## Преимущества

✅ Упрощённая структура данных  
✅ Более читаемый формат  
✅ Нет требований к изменению схемы БД  
✅ Полная информация об ответе в одном поле  
✅ Совместимо с API и фронтенд приложением  

## Поддерживаемые типы медиа в ответах

- 📝 Текстовые сообщения
- 🖼️ Изображения (используется caption)
- 🎬 Видео (используется caption)
- 📄 Документы (используется имя файла)
- 📁 Файлы без информации - отмечаются как `[Медиафайл]`
