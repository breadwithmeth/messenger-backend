# 🏷️ API Управления Тегами Клиентов

## Обзор

Система тегов позволяет организовывать и категоризировать клиентов с помощью гибких меток. Каждый тег имеет имя и цвет для визуального отображения.

## Эндпоинты

### 1. Получить все теги

**GET** `/api/clients/tags`

Возвращает список всех тегов организации с количеством связанных клиентов.

**Заголовки:**
```
Authorization: Bearer <token>
```

**Ответ (200):**
```json
[
  {
    "id": 1,
    "name": "VIP",
    "color": "#FFD700",
    "organizationId": 1,
    "createdAt": "2024-01-15T10:00:00.000Z",
    "_count": {
      "clients": 15
    }
  },
  {
    "id": 2,
    "name": "Оптовик",
    "color": "#4CAF50",
    "organizationId": 1,
    "createdAt": "2024-01-15T11:00:00.000Z",
    "_count": {
      "clients": 8
    }
  }
]
```

---

### 2. Получить тег по ID

**GET** `/api/clients/tags/:id`

Возвращает информацию о конкретном теге.

**Параметры URL:**
- `id` - ID тега

**Заголовки:**
```
Authorization: Bearer <token>
```

**Ответ (200):**
```json
{
  "id": 1,
  "name": "VIP",
  "color": "#FFD700",
  "organizationId": 1,
  "createdAt": "2024-01-15T10:00:00.000Z",
  "_count": {
    "clients": 15
  }
}
```

**Ошибки:**
- `404` - Тег не найден

---

### 3. Создать новый тег

**POST** `/api/clients/tags`

Создает новый тег для организации.

**Заголовки:**
```
Authorization: Bearer <token>
Content-Type: application/json
```

**Тело запроса:**
```json
{
  "name": "Новый клиент",
  "color": "#2196F3"
}
```

**Поля:**
- `name` (обязательное) - Название тега (уникальное в рамках организации)
- `color` (опциональное) - Цвет тега в формате HEX (например, #FF5733)

**Ответ (201):**
```json
{
  "id": 3,
  "name": "Новый клиент",
  "color": "#2196F3",
  "organizationId": 1,
  "createdAt": "2024-01-15T12:00:00.000Z"
}
```

**Ошибки:**
- `400` - Не указано название тега
- `409` - Тег с таким именем уже существует

---

### 4. Обновить тег

**PUT** `/api/clients/tags/:id`

Обновляет название или цвет тега.

**Параметры URL:**
- `id` - ID тега

**Заголовки:**
```
Authorization: Bearer <token>
Content-Type: application/json
```

**Тело запроса:**
```json
{
  "name": "Потенциальный VIP",
  "color": "#FFA500"
}
```

**Поля:**
- `name` (опциональное) - Новое название тега
- `color` (опциональное) - Новый цвет тега

**Ответ (200):**
```json
{
  "id": 3,
  "name": "Потенциальный VIP",
  "color": "#FFA500",
  "organizationId": 1,
  "createdAt": "2024-01-15T12:00:00.000Z"
}
```

**Ошибки:**
- `400` - Не указаны поля для обновления или невалидное значение
- `404` - Тег не найден
- `409` - Тег с таким именем уже существует

---

### 5. Удалить тег

**DELETE** `/api/clients/tags/:id`

Удаляет тег. При удалении тег автоматически открепляется от всех клиентов.

**Параметры URL:**
- `id` - ID тега

**Заголовки:**
```
Authorization: Bearer <token>
```

**Ответ (204):**
Пустое тело (No Content)

**Ошибки:**
- `404` - Тег не найден

---

### 6. Добавить тег клиенту

**POST** `/api/clients/:clientId/tags/:tagId`

Добавляет существующий тег клиенту.

**Параметры URL:**
- `clientId` - ID клиента
- `tagId` - ID тега

**Заголовки:**
```
Authorization: Bearer <token>
```

**Ответ (204):**
Пустое тело (No Content)

**Ошибки:**
- `404` - Клиент или тег не найден
- `400` - Невалидный ID

---

### 7. Удалить тег у клиента

**DELETE** `/api/clients/:clientId/tags/:tagId`

Убирает тег у клиента (сам тег остается в системе).

**Параметры URL:**
- `clientId` - ID клиента
- `tagId` - ID тега

**Заголовки:**
```
Authorization: Bearer <token>
```

**Ответ (204):**
Пустое тело (No Content)

**Ошибки:**
- `404` - Клиент не найден
- `400` - Невалидный ID

---

## Работа с тегами в API клиентов

### Получение клиентов с тегами

При запросе списка клиентов (`GET /api/clients`) или клиента по ID (`GET /api/clients/:id`), теги автоматически включаются в ответ:

```json
{
  "id": 123,
  "name": "Иван Петров",
  "email": "ivan@example.com",
  "phone": "+79001234567",
  "tags": [
    {
      "id": 1,
      "name": "VIP",
      "color": "#FFD700"
    },
    {
      "id": 2,
      "name": "Оптовик",
      "color": "#4CAF50"
    }
  ],
  ...
}
```

### Фильтрация клиентов по тегам

**GET** `/api/clients?tags=1,2`

Возвращает клиентов, у которых есть хотя бы один из указанных тегов.

**Query параметры:**
- `tags` - ID тега или массив ID тегов через запятую

**Примеры:**
```bash
# Клиенты с тегом VIP (ID=1)
GET /api/clients?tags=1

# Клиенты с тегом VIP или Оптовик
GET /api/clients?tags=1,2

# Клиенты с тегом VIP, страница 2
GET /api/clients?tags=1&page=2&limit=20
```

---

## Примеры использования

### Создание системы тегов для организации

```bash
# 1. Создать теги
curl -X POST http://localhost:3000/api/clients/tags \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"name": "VIP", "color": "#FFD700"}'

curl -X POST http://localhost:3000/api/clients/tags \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"name": "Оптовик", "color": "#4CAF50"}'

curl -X POST http://localhost:3000/api/clients/tags \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"name": "Проблемный", "color": "#F44336"}'
```

### Управление тегами клиента

```bash
# 2. Добавить теги клиенту
curl -X POST http://localhost:3000/api/clients/123/tags/1 \
  -H "Authorization: Bearer YOUR_TOKEN"

curl -X POST http://localhost:3000/api/clients/123/tags/2 \
  -H "Authorization: Bearer YOUR_TOKEN"

# 3. Получить информацию о клиенте с тегами
curl -X GET http://localhost:3000/api/clients/123 \
  -H "Authorization: Bearer YOUR_TOKEN"

# 4. Удалить тег у клиента
curl -X DELETE http://localhost:3000/api/clients/123/tags/2 \
  -H "Authorization: Bearer YOUR_TOKEN"
```

### Фильтрация и поиск

```bash
# Получить всех VIP клиентов
curl -X GET "http://localhost:3000/api/clients?tags=1" \
  -H "Authorization: Bearer YOUR_TOKEN"

# Получить VIP клиентов со статусом active
curl -X GET "http://localhost:3000/api/clients?tags=1&status=active" \
  -H "Authorization: Bearer YOUR_TOKEN"

# Поиск по тегу и имени
curl -X GET "http://localhost:3000/api/clients?tags=1&search=Иван" \
  -H "Authorization: Bearer YOUR_TOKEN"
```

### Аналитика по тегам

```bash
# Получить список всех тегов с количеством клиентов
curl -X GET http://localhost:3000/api/clients/tags \
  -H "Authorization: Bearer YOUR_TOKEN"
```

---

## Рекомендуемые практики

### Цветовая схема тегов

Используйте осмысленные цвета для визуального восприятия:

- 🟡 `#FFD700` - VIP, важные клиенты
- 🟢 `#4CAF50` - Активные, оптовики
- 🔵 `#2196F3` - Новые клиенты
- 🟠 `#FF9800` - Требуют внимания
- 🔴 `#F44336` - Проблемные
- 🟣 `#9C27B0` - Партнеры
- ⚫ `#607D8B` - Неактивные

### Стандартные теги

Рекомендуется создать базовый набор тегов:

1. **По статусу:**
   - Новый клиент
   - Активный
   - Неактивный
   - Потенциальный

2. **По типу:**
   - VIP
   - Оптовик
   - Розница
   - Партнер

3. **По поведению:**
   - Постоянный покупатель
   - Разовая покупка
   - Проблемный
   - Требует звонка

4. **По источнику:**
   - Из WhatsApp
   - Из Telegram
   - С сайта
   - По рекомендации

---

## Интеграция с фронтендом

### Компонент выбора тегов

```javascript
// Получение списка тегов для селектора
async function loadTags() {
  const response = await fetch('/api/clients/tags', {
    headers: {
      'Authorization': `Bearer ${token}`
    }
  });
  const tags = await response.json();
  return tags;
}

// Добавление тега клиенту
async function addTagToClient(clientId, tagId) {
  await fetch(`/api/clients/${clientId}/tags/${tagId}`, {
    method: 'POST',
    headers: {
      'Authorization': `Bearer ${token}`
    }
  });
}

// Удаление тега у клиента
async function removeTagFromClient(clientId, tagId) {
  await fetch(`/api/clients/${clientId}/tags/${tagId}`, {
    method: 'DELETE',
    headers: {
      'Authorization': `Bearer ${token}`
    }
  });
}
```

### Фильтр по тегам

```javascript
// Фильтрация клиентов по выбранным тегам
async function filterClientsByTags(selectedTagIds) {
  const tagsParam = selectedTagIds.join(',');
  const response = await fetch(`/api/clients?tags=${tagsParam}`, {
    headers: {
      'Authorization': `Bearer ${token}`
    }
  });
  const data = await response.json();
  return data.clients;
}
```

---

## Логирование

Все операции с тегами логируются с emoji-маркерами:

- 🏷️ - Получение тегов
- ➕ - Создание тега
- 📝 - Обновление тега
- ❌ - Удаление тега
- 🔗 - Добавление тега клиенту
- 🔓 - Удаление тега у клиента
- ✅ - Успешная операция
- ⚠️ - Предупреждение

Пример логов:
```
🏷️ Получение тегов организации {"organizationId": 1}
✅ Теги получены {"count": 5}

➕ Создание нового тега {"name": "VIP", "organizationId": 1}
✅ Тег создан {"tagId": 1, "name": "VIP"}

🔗 Добавление тега клиенту {"clientId": 123, "tagId": 1, "organizationId": 1}
✅ Тег добавлен клиенту {"clientId": 123, "tagId": 1}
```

---

## Безопасность

- Все эндпоинты требуют аутентификации через JWT токен
- Пользователь может работать только с тегами своей организации
- При удалении тега он автоматически открепляется от всех клиентов
- Проверка уникальности имени тега в рамках организации
- Валидация входных данных на стороне сервера

---

## База данных

### Модель ClientTag

```prisma
model ClientTag {
  id              Int      @id @default(autoincrement())
  organization    Organization @relation(fields: [organizationId], references: [id], onDelete: Cascade)
  organizationId  Int
  name            String   // Название тега
  color           String?  // Цвет в формате HEX
  createdAt       DateTime @default(now())
  
  clients         OrganizationClient[] @relation("ClientTags")
  
  @@unique([organizationId, name]) // Уникальность имени в рамках организации
}
```

### Связь с клиентами

```prisma
model OrganizationClient {
  // ... другие поля
  
  tags            ClientTag[] @relation("ClientTags")
}
```

---

## Миграции

Система тегов была добавлена в миграции `add_client_tags`. При необходимости откатить изменения:

```bash
npx prisma migrate reset
```

Для повторного применения:

```bash
npx prisma migrate deploy
```
