# Поддержка геолокации в WABA

**Дата:** 10 января 2026  
**Статус:** ✅ Реализовано

## Описание

Добавлена поддержка приема геолокации через WhatsApp Business API. Когда пользователь отправляет свою геолокацию в WhatsApp, система автоматически обрабатывает и сохраняет информацию.

## Структура входящего сообщения с геолокацией

```json
{
  "from": "77054810862",
  "id": "wamid.HBgLNzcwNTQ4MTA4NjIVAgASGBQzQTBCRDMyNUQ0MDhFNThENzczMQA=",
  "timestamp": "1767990010",
  "location": {
    "address": "улица Толстого",
    "latitude": 52.275764465332,
    "longitude": 76.964958190918,
    "name": "улица Толстого"
  },
  "type": "location"
}
```

## Обработка

Когда приходит сообщение с типом `location`, система:

1. **Извлекает данные геолокации**:
   - `latitude` - широта
   - `longitude` - долгота
   - `name` - название места
   - `address` - адрес

2. **Форматирует текстовое представление**:
   ```
   📍 улица Толстого
   Координаты: 52.275764465332, 76.964958190918
   Карта: https://www.google.com/maps?q=52.275764465332,76.964958190918
   ```

3. **Сохраняет в БД**:
   - `type: 'location'`
   - `content` - отформатированный текст с координатами и ссылкой на Google Maps

## Хранение данных

Геолокация сохраняется в существующие поля без изменения схемы БД:
- **type**: `'location'`
- **content**: Форматированный текст с названием, координатами и ссылкой на карту

## Примеры

### Геолокация с названием и адресом
```
Входящее: {name: "Кофейня", address: "ул. Ленина, 10"}
Сохраненное content:
📍 Кофейня, ул. Ленина, 10
Координаты: 52.123456, 76.123456
Карта: https://www.google.com/maps?q=52.123456,76.123456
```

### Геолокация только с координатами
```
Входящее: {latitude: 52.123456, longitude: 76.123456}
Сохраненное content:
📍 Геолокация
Координаты: 52.123456, 76.123456
Карта: https://www.google.com/maps?q=52.123456,76.123456
```

## Отображение на фронтенде

### Вариант 1: Простое отображение
Показать текст как есть:
```tsx
<div className="message-location">
  {message.content}
</div>
```

### Вариант 2: Интерактивная карта (рекомендуется)
Извлечь координаты из content и показать на карте:

```tsx
function LocationMessage({ message }) {
  // Извлекаем координаты из content
  const coordsMatch = message.content.match(/Координаты: ([\d.]+), ([\d.]+)/);
  const mapsUrlMatch = message.content.match(/Карта: (https:\/\/[^\s]+)/);
  
  if (!coordsMatch || !mapsUrlMatch) {
    return <div>{message.content}</div>;
  }
  
  const [, lat, lng] = coordsMatch;
  const [, mapsUrl] = mapsUrlMatch;
  
  return (
    <div className="location-message">
      <div className="location-header">
        📍 {message.content.split('\n')[0].replace('📍 ', '')}
      </div>
      <div className="location-coords">
        {lat}, {lng}
      </div>
      <a 
        href={mapsUrl} 
        target="_blank" 
        rel="noopener noreferrer"
        className="location-link"
      >
        Открыть в Google Maps
      </a>
    </div>
  );
}
```

### Вариант 3: Встроенная карта
```tsx
function LocationMessage({ message }) {
  const coordsMatch = message.content.match(/Координаты: ([\d.]+), ([\d.]+)/);
  if (!coordsMatch) return <div>{message.content}</div>;
  
  const [, lat, lng] = coordsMatch;
  
  return (
    <div className="location-message">
      <iframe
        width="300"
        height="200"
        frameBorder="0"
        src={`https://www.google.com/maps?q=${lat},${lng}&output=embed`}
        allowFullScreen
      />
      <a 
        href={`https://www.google.com/maps?q=${lat},${lng}`}
        target="_blank"
        rel="noopener noreferrer"
      >
        Открыть в Google Maps
      </a>
    </div>
  );
}
```

## Логирование

При получении геолокации в консоль выводится:
```
📍 WABA: Получена геолокация: {
  name: "улица Толстого",
  address: "улица Толстого",
  latitude: 52.275764465332,
  longitude: 76.964958190918,
  mapsUrl: "https://www.google.com/maps?q=52.275764465332,76.964958190918"
}
📍 WABA: Геолокация получена: улица Толстого (52.275764465332, 76.964958190918)
```

## API ответ

При получении сообщений через API:
```json
{
  "id": 123,
  "type": "location",
  "content": "📍 улица Толстого\nКоординаты: 52.275764465332, 76.964958190918\nКарта: https://www.google.com/maps?q=52.275764465332,76.964958190918",
  "timestamp": "2026-01-10T10:00:00.000Z",
  ...
}
```

## Преимущества решения

✅ Не требует изменений в схеме БД  
✅ Все данные доступны через существующий API  
✅ Ссылка на Google Maps сразу готова к использованию  
✅ Поддерживает названия и адреса локаций  
✅ Простая интеграция с фронтендом  
✅ Совместимо со всеми существующими API эндпоинтами

## Расширение в будущем

Если понадобится более продвинутая работа с геолокацией (например, поиск ближайших точек, отображение маршрутов), можно добавить отдельные поля в схему БД:
- `latitude: Float?`
- `longitude: Float?`
- `locationAddress: String?`
- `locationName: String?`

Но текущее решение работает без изменений в БД и покрывает основные случаи использования.
